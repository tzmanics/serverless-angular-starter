import { ScullyConfig } from '@scullyio/scully';
export const config: ScullyConfig = {
  projectRoot: "./src",
  projectName: "serverless-angular-starter",
  outDir: './dist/static',
  routes: {
  }
};